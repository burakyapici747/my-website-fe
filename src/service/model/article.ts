export type Article = {
    id: string,
    categoryId: string,
    title: string,
    content: string,
    rate: number,
    publishDate: string,
    coverImageURL: string,
    readingTime: number,
    htmlContent: string,
};