export type Article = {
    id: string,
    categoryId: string,
    title: string,
    description: string,
    rate: number,
    publishDate: string,
    coverImageURL: string,
    readTimeInMinute: number,
    htmlContent: string,
};